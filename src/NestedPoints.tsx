import React, {
    useCallback,
    useImperativeHandle,
    useMemo,
    useState,
} from 'react';
import { View } from 'react-native';

type NestedPointsProps = {
    offset: number;
};

const NestedPoints = ({ offset }: NestedPointsProps) => {
    return (
        <>
            <View></View>
        </>
    );
};
export default NestedPoints;
